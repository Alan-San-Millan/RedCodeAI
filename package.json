{
  "name": "reqcode-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Traduce requerimientos de software escritos en lenguaje natural a codigo funcional usando Google Gemini.",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check src/config.js && node --check src/lib/errors.js && node --check src/prompts/system-prompt.js && node --check src/services/gemini.service.js && node --check public/js/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google/genai": "^2.20.0",
    "@highlightjs/cdn-assets": "^11.12.0",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0"
  }
}
